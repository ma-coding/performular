import { AbstractFieldOptions } from '../../abstract-field/types/abstract-field-options';

export interface ControlFieldOptions extends AbstractFieldOptions {
    defaultValue?: any;
    value?: any;
}
